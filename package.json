{
  "name": "discord-bot",
  "version": "1.0.0",
  "description": "Simple ngrok tcp ip and port notifier to be sent to Discord bot. Module used : ngrok_info",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
